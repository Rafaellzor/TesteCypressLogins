# A minha automação Cypress

## Um pequeno prejeto feito no Cypress que automatiza uma feature de login e atualização de usuários.

## Rodando (necessário node instalado)
```bash
# Rodando com UI
npx cypress open

# Rodando pelo proprio terminal
npx cypress run
```
